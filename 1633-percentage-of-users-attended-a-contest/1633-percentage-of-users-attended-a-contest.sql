# Write your MySQL query statement below
SELECT r.contest_id,ROUND((COUNT(r.contest_id)/(SELECT COUNT(*) FROM Users))*100,2) AS percentage FROM Register as r GROUP BY contest_id ORDER BY percentage DESC, contest_id;